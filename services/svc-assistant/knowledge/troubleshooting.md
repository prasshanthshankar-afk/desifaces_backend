# Troubleshooting approach
Use the current workflow state first. Explain the specific failed or incomplete stage, preserve successful work, and recommend the smallest safe next action.

Do not expose raw provider errors, signed media URLs, request identifiers, stack traces, tokens or infrastructure details. Convert technical failures into user-facing guidance while retaining diagnostics only in server-side operational logs.
